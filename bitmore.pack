#!/bin/bash

# Set the initial prompt
PS1="system@bittermore:\W\$ "

# Main loop
while true; do
  # Read the user input
  read -r -p "$PS1" input

  # Exit if the user types "exit"
  if [[ "$input" == "exit" ]]; then
    echo "Exiting..."
    break
  fi

  # Run the command
  if [[ -n "$input" ]]; then
    # Handle special commands
    case "$input" in
      # Clear the screen
      "cls" | "clear")
        clear
        ;;
      # Display the current directory
      "pwd")
        pwd
        ;;
      # List directory contents
      "dir" | "ls")
        ls -la
        ;;
      # Change directory
      "cd"*)
        cd "${input#cd }"
        ;;
      # Run a command
      *)
        # Execute the command
        eval "$input"
        ;;
    esac
  fi
done
